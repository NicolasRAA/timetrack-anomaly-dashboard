# Обнаружение аномалий TimeTrack (автоэнкодер)

Streamlit-дашборд для датасета TimeTrack (`compute_dataset.csv`).

**Запуск локально:**
```bash
pip install -r requirements.txt
streamlit run app.py
```

**Деплой:** [Streamlit Community Cloud](https://share.streamlit.io) — главный файл `app.py`.
